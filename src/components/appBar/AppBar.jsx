import { Navigation } from "components/navigation/Navigation"

export const AppBar = () => {
    return <header>
        <Navigation />
         {/* {isLoggedIn ? <UserMenu /> : <AuthNav />} */}
    </header>
}